# AI Piano Arranger 重构调研（2026-07-30）

## 调研目标

结合 [Goal.md](/home/ithan/ai-piano-arranger/Goal.md) 里提出的两个核心问题，调研 2025/2026 年附近最相关的开源研究与代码仓库：

1. `SheetSage + Jukebox` 是否应该被 `MERT` 或更新一代的音乐理解模型替代？
2. 如何更合理地利用个人用户最容易收集的三类资源：
   - 原曲音频
   - 改编钢琴谱
   - 由改编钢琴谱渲染出的纯净钢琴 mp3
   
并尽量避免把整个流程强行压成 “OMR/识别 -> MIDI” 带来的信息损失？

本文件优先使用一手来源：官方论文、官方仓库、官方 README、官方项目页。GitHub 活跃度和 star 数以 2026-07-30 查询结果为准。

---

## TL;DR

先说结论：

1. `MERT` 仍然是一个很稳的开源替换候选，但它已经不是 2026 视角下唯一值得看的方案。  
   更前沿、也更值得比较的至少还有 `MuQ`，再往实验性一点还有 `Myna`。

2. 真正和你第二个问题最贴近的，不是某个“现成的钢琴改编大一统项目”，而是三条技术线的组合：
   - 音乐表征模型：`MERT / MuQ / MusicFM`
   - 跨模态表示与检索：`CLaMP3`
   - 转录/对齐/弱标注：`MT3 / YourMT3 / ai4m-miros / Audio2ScoreAlignment`

3. 我没有找到一个成熟的开源项目，能完整覆盖你想要的“三视图数据”：
   - 原曲音频
   - 改编钢琴谱
   - 改编钢琴谱渲染音频
   
   并且明确以“避免 OMR -> MIDI 信息塌缩”为目标。  
   这意味着你的重构方向不是“换个 repo 接上去”，而是要自己定义新的数据中心和中间表示。

4. 如果只是替换旧 `Jukebox`，`MERT` 是保守、安全、工程风险最低的答案。  
   如果要认真利用谱面和渲染钢琴音频，`CLaMP3` 这类“把音频 / 谱面 / MIDI / 图像放进共享表示空间”的路线，比单纯的 `MERT` 替换更接近你真正的问题。

5. 我当前的判断是：  
   你后续真正要选的，不是 “MERT 还是不 MERT”，而是：
   - 只做音频 encoder 升级
   - 还是把整个项目改成“三视图对齐 + 改编生成”的新架构

---

## 1. 2025/2026 最相关的开源方向

### 1.1 音乐音频表征模型（替代 Jukebox / 补全 SheetSage 前端）

| 项目 | 论文/年份 | 模态 | 开源成熟度 | 为什么相关 | 主要注意点 |
|---|---|---|---|---|---|
| `MERT` | 2023, arXiv 2306.00107 | 音频 | 高 | 你在 `Goal.md` 里已经指向它；目前仍是音乐理解常用基线 | 主要是音频 encoder，不直接解决谱面利用问题 |
| `MusicFM` | 2024, arXiv 2311.03318 | 音频 | 中高 | 明确强调 frame-level / sequence-level embedding，可用于 beat/chord/structure 等任务 | 仓库较活跃，但生态不如 MERT 广；许可信息需单独再核对 |
| `MuQ` | 2025, arXiv 2501.01108 | 音频；另有音频-文本 `MuQ-MuLan` | 高 | 官方直接把它定位成较新的音乐 foundation model，并声称在多项 MIR 任务上达到 SOTA | 权重许可是 `CC-BY-NC 4.0`，商用限制要注意 |
| `Myna` | 2025 | 音频 | 低到中 | 很新的表征学习路线，值得关注研究思路 | 社区体量很小，离“稳工程依赖”还有距离 |
| `MARBLE` | benchmark | 评测，不是模型 | 高 | 方便你后续做 MERT vs MuQ vs MusicFM 的本地可复现实验 | 不直接产出改编能力 |

#### 观察

- `MERT` 是稳妥替换方案。  
- `MuQ` 是“2025 以后更前沿”的强候选。这个判断主要来自其官方 repo 和 paper 对 MARBLE 等 MIR 任务表现的描述；是否比 `MERT` 更适合你的任务，仍需要你自己的本地评测验证。
- `MusicFM` 的优点是它非常明确地讨论了不同下游任务需要不同时间分辨率，这点对你后续要保留节拍、结构、和声变化很重要。
- `Myna` 目前更像“值得追踪的研究苗头”，不像可以直接接管核心生产链路的成熟底座。

#### 对你的问题的含义

如果问题只是“拿一个更好的 encoder 取代旧 Jukebox”，那么：

- 保守路线：`MERT`
- 前沿路线：`MERT vs MuQ` 做并行评估
- 实验路线：把 `MusicFM` 也放进基线比较

---

### 1.2 跨模态音乐表示（音频 / 谱面 / MIDI / 图像）

这是本次调研里最重要的一条线，因为它直接碰到你第二个问题。

| 项目 | 论文/年份 | 支持模态 | 开源成熟度 | 为什么相关 | 主要注意点 |
|---|---|---|---|---|---|
| `CLaMP3` | ACL 2025, arXiv 2502.10362 | 文本 / Sheet Music / Audio / MIDI / Images | 高 | 这是我找到的最像“把谱面当一等公民”的开源项目 | 它是跨模态检索/表示框架，不是钢琴改编生成器 |
| `Audio2ScoreAlignment` | 2021 | 音频 / score 对齐 | 中 | 适合把演奏音频和谱面对齐，为数据构建服务 | 偏工具链，不是 foundation model |

#### 为什么 `CLaMP3` 很关键

`CLaMP3` 官方 README 明确写了：

- 支持 `text / sheet music / audio / MIDI / images`
- 支持 `MusicXML`、图片、音频、MIDI 等输入
- 使用共享表示空间做跨模态检索
- 其音频侧预处理本身就使用 `MERT` 特征

这件事的意义很大：

1. 它证明了“音频、谱面、MIDI、图像”放进同一个表示空间，是一个已经被开源实现过的方向。
2. 它说明“把谱面直接看成另一种模态”，在今天已经比“先 OMR 成 MIDI 再说”更前沿。
3. 它和你的三类个人资源天然对得上：
   - 原曲音频 -> audio modality
   - 改编钢琴谱 -> sheet / image modality
   - 渲染钢琴 mp3 -> audio modality（但语义更接近 piano arrangement）

#### 但 `CLaMP3` 也不是现成答案

它解决的是：

- 跨模态表示
- 检索
- 语义相似度
- zero-shot 分类

它没有直接解决：

- 原曲到钢琴改编生成
- 钢琴谱细粒度控制
- 你项目里已有 decoder 的条件生成问题

所以它更像是：

- 一个新的“数据中心思想”
- 一个很重要的参考架构
- 一个说明“别把谱面降格成只剩 MIDI”的强证据

而不是可直接替代 `PiCoGen2` 的现成生成系统。

---

### 1.3 转录、对齐、弱标注（为三视图数据服务）

| 项目 | 年份 | 任务 | 开源成熟度 | 为什么相关 | 主要注意点 |
|---|---|---|---|---|---|
| `MT3` | 2021/2022 | 多任务多音轨 AMT | 高 | 最经典的大模型转录基线之一 | 官方 repo 对训练支持一般，更偏推理/已有 checkpoint |
| `YourMT3` / `YourMT3+` | 2022 / 2024 | 更灵活的多任务多音轨转录 | 中高 | 社区常拿它做可扩展 AMT 基座 | `GPL-3.0` 许可需要注意 |
| `ai4m-miros` | 2025 | 竞赛级转录系统 | 中 | 官方写明是 2025 AI4Musician 转录竞赛冠军方案，并扩展了 `YourMT3+`，加了 pretrained encoder | 很新，社区体量还小，但方向非常 relevant |
| `Basic Pitch` | 2022 | 轻量级 audio-to-MIDI | 很高 | 工程上很方便，生态很大 | 对你的目标来说太轻，会把问题再次压缩回“先转 MIDI” |
| `Audio2ScoreAlignment` | 2021 | 对齐 | 中 | 能服务于音频与谱面/转录结果的对齐 | 本身不是生成模型 |

#### 为什么这条线对你很重要

你不想再依赖 `HOMR` 这类 OMR 把谱面硬转成 MIDI，这是合理的。  
但这不代表你完全不需要“转录 / 对齐”技术。

更合理的用法是：

- 不把转录结果当作最终真值
- 只把它当作数据对齐工具、弱标注工具、辅助索引工具

特别是你的第三类资源“由钢琴谱渲染出的纯净钢琴 mp3”很有价值：

1. 它比原曲混音更适合做高质量 AMT。
2. 它比从扫描谱面做 OMR 稳得多。
3. 它可以作为谱面和音频之间的桥梁。

这里最值得注意的，不是 “要不要 AMT”，而是：

- 对原曲：AMT 只能作为弱信息
- 对渲染钢琴 mp3：AMT 可以作为高质量辅助信息
- 对钢琴谱本体：最好保留原始谱面文件、图片、分页结构、符号信息，而不是只留下 MIDI

这是我根据这些项目组合后得出的工程判断，不是某一篇论文的直接结论。

---

### 1.4 原曲到钢琴改编 / Piano Cover Generation

| 项目 | 论文/年份 | 路线 | 开源成熟度 | 为什么相关 | 主要注意点 |
|---|---|---|---|---|---|
| `Pop2Piano` | 2022, arXiv 2211.00895 | 原曲音频 -> 钢琴 cover | 高 | 最经典的开源 baseline 之一 | 偏旧，不处理你的“三视图数据中心”问题 |
| `PiCoGen` / `PiCoGen2` | 2024, arXiv 2407.20883 / 2408.01551 | v1 两阶段，v2 端到端 | 高 | 就是你当前项目的直接祖先路线 | 你现在正是要重构它 |
| `AMT-APC` | 2024, arXiv 2409.14086 | 微调 AMT 模型做自动钢琴 cover | 中高 | 这是我本次调研里最贴近你目标的新参考之一 | 本质仍强依赖转录型表征，不等于真正的谱面中心架构 |

#### `AMT-APC` 为什么值得认真看

`AMT-APC` 官方就写得很直接：  
它是“通过微调 AMT 模型来做 Automatic Piano Cover”。

这条路线和你的重构目标有天然亲缘性：

- 都想摆脱老的两阶段弱点
- 都说明转录模型本身可以成为生成的前端/骨架
- 都在试图把“音乐理解”往更结构化的音高-时间表示靠拢

但它也有一个明显局限：

- 它更像是“把改编建在 AMT 模型之上”
- 而你真正想探索的，是“把谱面、渲染钢琴音频、原曲音频一起建模”

换句话说，`AMT-APC` 很值得借鉴，但它更像你的中间参考，不像最终目标。

---

## 2. 从你的问题出发，现有开源版图到底说明了什么

### 2.1 关于“是否用 MERT 替代 Jukebox”

答案是：可以，而且很合理。  
但如果你以 2026 年视角做这件事，最好别只看 `MERT`。

更准确的表述应该是：

- `MERT` 是稳妥替代
- `MuQ` 是更前沿的强竞争者
- `MusicFM` 在时间分辨率设计上也值得做基线

如果你后面要做一次正式选择，我建议把这个问题写成：

> 新前端音乐表示层是否采用 `MERT-only`，还是至少在 `MERT / MuQ / MusicFM` 中做一次小规模对比实验？

这比直接把答案写死成 `MERT` 更稳。

---

### 2.2 关于“如何利用 原曲 / 钢琴谱 / 渲染钢琴音频 三类资源”

这是当前项目真正该重构的核心。

从调研结果看，更合理的思路不是把三者都转成 MIDI，而是把它们看成三个互补视角：

1. `原曲音频`
   - 提供风格、编制、动态、段落结构、和声色彩、旋律显著性
   - 但不适合作为唯一的符号监督来源

2. `改编钢琴谱`
   - 提供最强的结构监督
   - 包含指法、分声部、和弦织体、布局、重复记号、力度记号等 MIDI 常常丢失或弱化的信息

3. `渲染钢琴 mp3`
   - 是谱面到音频的“干净桥梁”
   - 可以做高质量对齐、AMT、表示蒸馏、对比学习
   - 比从原曲直接转录容易得多

这三者的关系更像：

- 原曲音频：目标语义源
- 钢琴谱：结构真值源
- 渲染钢琴音频：跨模态对齐桥梁

这套思路和 `CLaMP3 + AMT/Alignment + Arrangement Decoder` 的组合最贴近。

---

### 2.3 为什么“避免 OMR -> MIDI 信息塌缩”是对的

这次调研没有找到一个强有力的证据说明：

> 先把谱面识别成 MIDI，再把 MIDI 当作唯一真值  
> 会是 2026 年最好的做法

相反，更前沿的开源信号都在提示另一件事：

- `CLaMP3`：谱面、图片、音频、MIDI 应该并列建模
- `AMT-APC`：转录模型可以成为生成入口，但不必是唯一表示
- `PiCoGen2`：端到端是在试图绕开两阶段损失

所以从今天的公开版图看，你的问题意识是对的：

> “谱面不应只是一个为了变成 MIDI 而存在的中间输入。”

---

## 3. 我认为最值得深看的 shortlist

如果只保留最有决策价值的几个项目，我会选这 8 个：

### A. 表征模型 shortlist

1. `MERT`
2. `MuQ`
3. `MusicFM`
4. `MARBLE`

用途：

- 决定新前端 encoder 的候选池
- 设计本地 benchmark

### B. 谱面 / 跨模态 shortlist

5. `CLaMP3`
6. `Audio2ScoreAlignment`

用途：

- 决定你的“数据中心”是不是要变成多模态对齐
- 决定谱面是否保留为 first-class modality

### C. 改编 / 生成 shortlist

7. `AMT-APC`
8. `PiCoGen2`（作为现系统祖先对照）

用途：

- 决定生成部分是继续沿用旧 decoder 思想，还是往 AMT-like front-end / multi-view front-end 迁移

如果只从“最像你要走的方向”排序，我会这样排：

1. `CLaMP3`
2. `MuQ`
3. `AMT-APC`
4. `MERT`
5. `PiCoGen2`

这里的排序不是“谁最强”，而是谁对你的重构决策最有启发。

---

## 4. 对后续 A / B / C 选择的含义

这里先不替你拍板，只写调研后的判断框架。

### 方案 A：最小侵入替换

特征：

- 保留当前大部分 `PiCoGen2` 后端
- 只把 `SheetSage/Jukebox` 替换成新的 audio encoder

调研对应：

- 最适合拿 `MERT`
- 也应该把 `MuQ` 放进对比

优点：

- 工程风险低
- 最快出实验结果

缺点：

- 不能真正回答你第二个问题
- 基本没有把“钢琴谱”和“渲染钢琴音频”的独特价值吃干净

### 方案 B：中等重构，建立“三视图数据中心”

特征：

- 不再把谱面视作最终必须坍缩为 MIDI 的东西
- 原曲音频 / 钢琴谱 / 渲染钢琴音频 三者并列建模
- 生成器仍可沿用现有 decoder 思路，但前端表示和数据构建重写

调研对应：

- `MuQ or MERT` 作为音频编码候选
- `CLaMP3` 提供跨模态思路
- `Audio2ScoreAlignment + AMT` 用于对齐和弱标注

优点：

- 最符合你这次真正的重构诉求
- 既有工程落地性，又有研究创新性

缺点：

- 数据设计和训练流程会明显变复杂

### 方案 C：激进重构，走 multi-view arrangement foundation route

特征：

- 直接把项目定义成“多模态钢琴改编系统”
- 表征层、对齐层、生成层都重写

调研对应：

- 最接近 `CLaMP3 + MuQ + custom decoder`

优点：

- 方向最前沿

缺点：

- 工程成本最高
- 需要你先把数据中间表示定义清楚，否则会在实现期发散

---

## 5. 当前最重要的研究空白

我这次没有找到一个成熟开源项目，能同时满足下面四件事：

1. 输入原曲音频
2. 利用改编钢琴谱本体，而不是只用它导出的 MIDI
3. 利用改编钢琴谱渲染出的纯净钢琴音频
4. 最终服务于钢琴改编生成

这意味着你的项目真正的创新点，很可能不是“用了哪个 encoder”，而是：

> 你如何定义这三类资源之间的关系，  
> 以及你是否把它们组织成一个新的训练样本单位。

这是后面写新规划方案时最该抓住的地方。

---

## 6. 我现在的倾向性判断

这部分是我的综合判断，不是任何单一来源的原话。

### 我认同的判断

1. `Jukebox` 应该退场。  
2. 只换成 `MERT` 不够，至少应该把 `MuQ` 纳入候选。  
3. 你的真正突破口不在“更强 AMT”，而在“把谱面和渲染钢琴音频变成一等训练资源”。  
4. `CLaMP3` 是本次调研里最值得偷师的数据观和模态观。  
5. `AMT-APC` 是最值得偷师的“从转录模型走向钢琴改编”的生成参考。  

### 我暂时不建议直接做的事

1. 直接把 `Goal.md` 里的 `MERT -> BeatSyncPool -> DistillationAdapter` 当成最终方案写死。  
   这个想法有可取之处，但它更像“旧接口保守升级”，还没有真正回答第二个问题。

2. 把谱面继续主要视作“想办法变成 MIDI 的东西”。  
   这和当前开源前沿最有价值的信号是反着走的。

---

## 7. 参考来源

### 论文 / 项目页

- MERT: https://arxiv.org/abs/2306.00107
- MusicFM: https://arxiv.org/abs/2311.03318
- MuQ: https://arxiv.org/abs/2501.01108
- CLaMP3: https://arxiv.org/abs/2502.10362
- Pop2Piano: https://arxiv.org/abs/2211.00895
- PiCoGen: https://arxiv.org/abs/2407.20883
- PiCoGen2: https://arxiv.org/abs/2408.01551
- AMT-APC: https://arxiv.org/abs/2409.14086
- YourMT3+: https://arxiv.org/abs/2407.04822

### 官方仓库

- MERT: https://github.com/yizhilll/MERT
- MusicFM: https://github.com/minzwon/musicfm
- MuQ: https://github.com/tencent-ailab/MuQ
- Myna: https://github.com/ghost-signal/myna
- MARBLE: https://github.com/a43992899/MARBLE
- CLaMP3: https://github.com/sanderwood/clamp3
- MT3: https://github.com/magenta/mt3
- YourMT3: https://github.com/mimbres/YourMT3
- ai4m-miros: https://github.com/amt-os/ai4m-miros
- Basic Pitch: https://github.com/spotify/basic-pitch
- Audio2ScoreAlignment: https://github.com/LIMUNIMI/MMSP2021-Audio2ScoreAlignment
- Pop2Piano: https://github.com/sweetcocoa/pop2piano
- PiCoGen: https://github.com/tanchihpin0517/PiCoGen
- AMT-APC: https://github.com/310hz/amt-apc

---

## 8. 下一步建议

基于这份调研，下一步最合理的动作不是立刻改代码，而是先写一版新的重构规划，明确：

1. 新的数据样本单位怎么定义
2. 哪些信息永远不再坍缩成单一 MIDI
3. 前端 encoder 候选池是否限定为 `MERT / MuQ / MusicFM`
4. 生成器是延续现有 `PiCoGen2` 后端，还是做更大幅度重构

我建议下一份文档直接围绕这些问题来写，而不是继续在旧 `SheetSage` 接口上修修补补。
